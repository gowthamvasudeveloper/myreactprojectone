"""
Backend application package.

Why this file exists:
- It marks `app/` as a Python package so imports like `from app.core.settings import settings` work.
- It provides a stable import root for the rest of the backend.
"""

